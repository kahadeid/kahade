--- a/backend/src/core/order/order.service.ts
+++ b/backend/src/core/order/order.service.ts
@@ -389,7 +389,7 @@
     // Check balance
     const balance = await this.walletService.getBalance(order.initiatorId);
 
-    if (BigInt(balance.available) < totalAmount) {
+    if (BigInt(Math.round(balance.balance * 100)) < totalAmount) {
       throw new BadRequestException('Insufficient balance');
     }
