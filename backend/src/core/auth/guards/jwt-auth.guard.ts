/**
 * JWT Auth Guard - Core Auth
 *
 * Re-exports JwtAuthGuard from common/guards for backward compatibility.
 * Code should prefer importing from @common/guards/jwt-auth.guard directly.
 */
export { JwtAuthGuard } from '@common/guards/jwt-auth.guard';
