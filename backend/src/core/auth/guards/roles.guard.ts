/**
 * Roles Guard - Core Auth
 *
 * Re-exports RolesGuard from security/guards for backward compatibility.
 * Code should prefer importing from @security/guards/roles.guard directly.
 */
export { RolesGuard } from '@security/guards/roles.guard';
