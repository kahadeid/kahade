## Legacy Express Routes

The Express-style route stubs that previously lived in this folder were removed to prevent
accidental routing to 501 placeholders. The NestJS controllers under `backend/src` are the
source of truth for API routing going forward.
